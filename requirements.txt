# FSLDM Data SDLC Agents — Python dependencies

# ── LangGraph + LangChain ─────────────────────────────────────
langgraph>=0.2.0
langchain>=0.3.0
langchain-anthropic>=0.2.0
langchain-core>=0.3.0

# ── Anthropic SDK ─────────────────────────────────────────────
anthropic>=0.40.0

# ── SQL validation ────────────────────────────────────────────
sqlglot>=25.0.0

# ── Data validation / testing ─────────────────────────────────
great-expectations>=1.0.0
soda-core>=3.3.0
soda-core-teradata>=3.3.0      # Teradata adapter for Soda

# ── Excel mapping spec generation ────────────────────────────
openpyxl>=3.1.0

# ── CLI ───────────────────────────────────────────────────────
typer>=0.12.0
rich>=13.0.0

# ── dbt (AWS migration path) ──────────────────────────────────
dbt-core>=1.8.0
dbt-snowflake>=1.8.0           # AWS Snowflake target
# dbt-redshift>=1.8.0          # Uncomment for Redshift

# ── Pydantic ─────────────────────────────────────────────────
pydantic>=2.0.0

# ── Dev / testing ─────────────────────────────────────────────
pytest>=8.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# ── OCBC LLM integration ───────────────────────────────────────
# Already covered by requests above
